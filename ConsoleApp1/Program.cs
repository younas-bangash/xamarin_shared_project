using System;
using SharedProject;

namespace ConsoleApp
{
    class Program
    {
        static void Main(string[] args)
        {
            // Create object of the string class
            Strings strings = new Strings();

            System.Diagnostics.Debug.WriteLine(strings.newLine);
        }
    }
}
